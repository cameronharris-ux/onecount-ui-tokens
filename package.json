{
  "name": "@onecount/ui-tokens",
  "version": "0.5.0",
  "description": "Canonical OneCount design tokens - audited per-app theme values, shared schema constants, and zero-dep drift/contrast checks.",
  "license": "UNLICENSED",
  "private": false,
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist",
    "tokens.json",
    "src",
    "scripts"
  ],
  "scripts": {
    "build": "tsc && cp tokens.json dist/tokens.json",
    "test": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
